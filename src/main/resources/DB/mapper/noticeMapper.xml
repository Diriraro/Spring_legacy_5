<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.iu.s5.notice.NoticeDAO">
  
  	<sql id="search">
  	like '%'||#{search}||'%'
  	</sql>
  
  		<!-- 전체 글의 갯수 -->
  		<select id="boardCount" parameterType="Pager" resultType="Long">
  			select count(num) from notice 
  			<where>
  				<if test="kind=='bt'">
  				title <include refid="search"></include>
  				</if>
  				<if test = "kind == 'bw'">
  				writer <include refid="search"></include>
  				</if>
  				<if test = "kind=='bc'"> 
  				contents <include refid="search"></include>
  				</if>
  			</where>
  			
  			
  		</select>
  
  		<select id="boardList" parameterType="Pager" resultType="NoticeVO">
			select * from  
				(select N.*, rownum R from 
					(select * from notice where 
					<choose>
						<when test="kind == 'bw'">
							writer
						</when>
						<when test="kind == 'bt'">
							title						
						</when>
						<otherwise>
							contents
						</otherwise>
					</choose>
					
					like '%'||#{search}||'%' and num >=1 order by num desc) N)
			where R between #{startRow} and #{lastRow}

  		</select>
  
  		<select id="boardSelect" parameterType="Long" resultType="NoticeVO">
  			SELECT * FROM notice WHERE num=#{num}
  		</select>
  		
  		<insert id="boardWrite" parameterType="NoticeVO">
  			INSERT INTO notice VALUES(board_seq.nextval, #{title}, #{writer}, #{contents}, sysdate, 0)
  		</insert>
  		
  		<delete id="boardDelete" parameterType="Long">
  			DELETE notice WHERE num=#{num}
  		</delete>
  		
  		<update id="boardUpdate" parameterType="NoticeVO">
  			UPDATE notice SET title=#{title}, contents=#{contents}, hit=hit-1 WHERE num=#{num}
  		</update>
  		
  		<update id="hitUpdate" parameterType="Long">
  			UPDATE notice SET hit=hit+1 WHERE num=#{num}
  		</update>
  		
  
  
  </mapper>